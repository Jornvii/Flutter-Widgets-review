import 'package:flutter/material.dart';

class Widget00 extends StatelessWidget {
  const Widget00({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("A"),
      ),
      body: Center(
        
      ),
    );
  }
}
